{
  "nodes": [
    {
      "id": "prepare",
      "role": "test",
      "objective": "emit deterministic fixture setup evidence",
      "command": ["python", "-c", "print('fixture-success:prepare')"]
    },
    {
      "id": "verify",
      "role": "verify",
      "objective": "emit deterministic fixture verification evidence",
      "dependencies": ["prepare"],
      "command": ["python", "-c", "print('fixture-success:verify')"]
    }
  ],
  "edges": [["prepare", "verify"]]
}
